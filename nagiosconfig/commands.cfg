define command{
  command_name                    cb_ops_per_sec
  command_line $USER1$/check_couchbase_server.py -u admin -p passwd -I cb_ip  -P cb_port -b bucket_name -s server_ip --OPS -W $ARG1$ -C $ARG2$
  }

define command{
  command_name                    cb_item_count
  command_line $USER1$/check_couchbase_server.py -u admin -p passwd -I cb_ip  -P cb_port -b bucket_name -s server_ip --OPS -W $ARG1$ -C $ARG2$
  }
  
define command{
  command_name                    cb_mem_usage
  command_line $USER1$/check_couchbase_server.py -u admin -p passwd -I cb_ip  -P cb_port -b bucket_name -s server_ip --OPS -W $ARG1$ -C $ARG2$
  }

define command{
  command_name                    cb_disk_read
  command_line $USER1$/check_couchbase_server.py -u admin -p passwd -I cb_ip  -P cb_port -b bucket_name -s server_ip --OPS -W $ARG1$ -C $ARG2$
        }
  
define command{
  command_name                    cb_cas
  command_line $USER1$/check_couchbase_server.py -u admin -p passwd -I cb_ip  -P cb_port -b bucket_name --cbstat  /cb_stat_path/-s server_ip --OPS -W $ARG1$ -C $ARG2$
  }

define command{
  command_name                    cb_del_per_sec
  command_line $USER1$/check_couchbase_server.py -u admin -p passwd -I cb_ip  -P cb_port -b bucket_name --cbstat  /cb_stat_path/-s server_ip --OPS -W $ARG1$ -C $ARG2$
  }

define command{
  command_name                    cb_low_watermark
  command_line $USER1$/check_couchbase_server.py -u admin -p passwd -I cb_ip  -P cb_port -b bucket_name --cbstat  /cb_stat_path/-s server_ip --OPS -W $ARG1$ -C $ARG2$
  }

define command{
  command_name                    cb_high_watermark
  command_line $USER1$/check_couchbase_server.py -u admin -p passwd -I cb_ip  -P cb_port -b bucket_name --cbstat  /cb_stat_path/-s server_ip --OPS -W $ARG1$ -C $ARG2$
  }

